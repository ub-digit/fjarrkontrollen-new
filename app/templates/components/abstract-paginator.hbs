{{yield
  (hash
    isFirstPage=isFirstPage
    isLastPage=isLastPage
    nextPageAction=(action "nextPage")
    previousPageAction=(action "previousPage")
    changePageAction=(action "changePage")
    pageItems=pageItems
  )
}}
